# ═══════════════════════════════════════
#        CLAW GAME — .env CONFIG
# ═══════════════════════════════════════
# Copy this to .env and fill in your values
# NEVER commit .env to git!

# ── Your MetaMask private key (WITHOUT the 0x prefix) ──
# MetaMask > ⋮ > Account details > Export private key
PRIVATE_KEY=

# ── BaseScan API key (for contract verification) ──
# Get one at basescan.org > API Keys
BASESCAN_API_KEY=

# ── Token address ($GAME) ──
# Testnet: set after deploy-testnet.js runs
# Mainnet: set after creating token on flaunch.gg
GAME_TOKEN_ADDRESS=0x...

# ── Contract address (ClawGame) ──
# Set after deploy script runs
CONTRACT_ADDRESS=0x...

# ── Your wallets ──
TREASURY_WALLET=0xYOUR_WALLET_ADDRESS
LP_WALLET=0xYOUR_WALLET_ADDRESS

# ── Network ──
# Testnet: https://sepolia.base.org  (chainId 84532)
# Mainnet: https://mainnet.base.org  (chainId 8453)
BASE_RPC_URL=https://sepolia.base.org
CHAIN_ID=84532

# ── API Server ──
DATABASE_URL=sqlite+aiosqlite:///./clawgame.db
API_HOST=0.0.0.0
API_PORT=8000

# ── Token price (manual for V1, oracle later) ──
GAME_PRICE_ETH=0.000002
